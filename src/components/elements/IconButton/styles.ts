import styled from 'styled-components';

export const IconButtonContainer = styled.div.attrs({
  className:
    'flex items-center justify-center w-8 h-8 cursor-pointer hover:bg-black/10 transition rounded',
})``;
